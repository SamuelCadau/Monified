// INDEX FOR SERVICES TO FACILITATE IMPORTS LIKE MODELS

const fs = require("fs");
const path = require("path");
const Sequelize = require("sequelize");
const basename = path.basename(__filename);

// IMPORT LIKE THAT const services = require("./services/index")["ServiceName"];
